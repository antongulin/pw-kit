# Built plugin artifacts
*.plugin
*.zip
dist/

# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
!.vscode/mcp.json
.idea/

# Node (in case someone runs npm here)
node_modules/
package-lock.json
npm-debug.log*

# Python (for skill-creator helper scripts)
__pycache__/
*.pyc
*.pyo
.venv/

# Eval workspace artifacts (kept local)
*-workspace/
eval-results/

# Serena artifacts — retired; ignore guard kept so a stale local .serena/
# can never be committed. CodeGraph replaced Serena as the code-intelligence tool.
.serena/

# CodeGraph local index and daemon state; the MCP config files are committed.
.codegraph/
